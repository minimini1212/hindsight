# TODOS — 지금 상태와 남은 일

> 🔴 **이 파일의 맨 위 절이 「지금 어디까지 왔나」의 유일한 근거다.**
> 규율은 [CLAUDE.md](CLAUDE.md), 무엇을 만드는가는 [docs/DESIGN.md](docs/DESIGN.md).

---

## 지금 상태 (2026-09-15)

**🔄 기록을 «만들고», 그 기록으로 «채점»하는 데까지 왔다. 테스트 120개 통과.**

| | |
| --- | --- |
| 문서 | PRD · DESIGN · DATA_CONTRACT · 규율 · 결정 기록 **일곱** · 실측 기록 **일곱** · 리뷰 하나 |
| 코드 | `hindsight-model`(자료 구조) · `hindsight-core`(읽기·쓰기 + 🔄 가명화) · 🔄 `hindsight-recorder-simple`(v0 기록기) · `demo-app`(관측 대상 + 실험) |
| 모듈 | 계획 6개 중 **4개가 디스크에 있다**. 🔄 v0 기록기를 **모듈로 정했다** (2026-09-15, 재서) |
| 빌드 | ✅ Gradle 9.3 · **이중 툴체인 실측 확인** — model major=61(Java 17), core major=65(Java 21) |
| 테스트 | ✅ **120개 통과** — core 56 · recorder-simple 43 · demo-app 21(실험 ①~⑪) |
| 검사 | ✅ **넷 다 «일부러 깨뜨려» 막는 것 확인** — 에이전트 의존성 · 문서 링크 · 왕복 · 🔄 기록기 의존성 |

🔴 **다음 할 일: 재생을 실제로 «돌리는» 엔진.** 채점 기준은 섰다 — 무엇으로 「맞았다」를
판정할지(오라클 셋)와 이 재생을 얼마나 믿을지(등급)가 순수 함수로 있다.
⬜ **아직 없는 것은 기록을 앱에 다시 흘려 넣는 코드**다. 지금은 「재생해 보니 이랬다」를
사람이 넣어 줘야 한다 ([replay-oracle.md](docs/reports/2026-09-15/replay-oracle.md)).

🎯 **성능은 재서 목표 안에 넣었다** — SQL 이벤트가 5.42µs 로 목표(2µs)를 넘겼는데,
비싼 자리를 «재서» 찾아 **0.46µs** 로 줄였다. 고칠 자리는 링 버퍼가 아니라 요약층이었다
([recorder-overhead.md](docs/reports/2026-09-15/recorder-overhead.md)).

⚠️ **기록기를 만들면서 나온 가장 값나가는 것**: 따로 떼어 한 검사 28건이 전부 통과했는데도
진짜 앱에 붙이니 결함 둘이 나왔다 — 예외를 감싸는 쪽과 같이 도는 쪽이 있어야 드러나는
종류였다. 🧭 [v0-recorder.md](docs/reports/2026-09-15/v0-recorder.md)

---

## 🔴 먼저 확인해야 결정이 유지되는 것

⚠️ **이 둘이 아니라고 나오면 계획을 다시 짠다.**

- ✅ **`Filter` + `DataSource` 감싸기로 만든 기록으로 재생이 성립하는가**
  — 🎯 **2026-09-11 실측: 성립한다.** 본문을 잡고도 앱이 200 을 돌려주고, 잡은 것«만»으로
  재생하니 읽기 요청은 **글자까지 동일**했다. 바이트코드를 v1 로 미룬 근거가 유지된다.
  🧭 [reports/2026-09-11/http-capture-experiment.md](docs/reports/2026-09-11/http-capture-experiment.md)
- ✅ **오라클(설계 §6-1)이 실제로 N+1을 잡는가** — 🎯 **2026-09-11 실측: 잡는다.**
  주문 20건 → SQL 21번(1+20), 두 번 돌려도 21/21, **두 배로 안 세인다.**
  🔄 그리고 「모양별 반복」이 「총 횟수」보다 나은 신호라는 걸 알아내 오라클을 다듬었다.
  🧭 [reports/2026-09-11/sql-count-experiment.md](docs/reports/2026-09-11/sql-count-experiment.md)
- ✅ 🔄 **쓰기 요청도 재생으로 채점할 수 있는가** — 🎯 **2026-09-11 실측: 할 수 있다.**
  「쓰기라서 다르다」가 아니라 **「DB 를 안 되돌려서 다르다」**였다. 행 + 자동 증가 카운터까지
  되돌리니 **글자까지 같은 응답**이 나왔다. 🔴 행만 되돌리면 재생이 **예외로 죽는다** —
  부분 복원은 무복원보다 위험하다.
  🧭 [reports/2026-09-11/write-replay-experiment.md](docs/reports/2026-09-11/write-replay-experiment.md)

---

- ✅ 🔄 **되돌리는 «방법»을 무엇으로 하나** — 🎯 **2026-09-11 실측: 스냅숏.**
  트랜잭션 롤백은 행만 되돌려서 재생이 `id` 4·5·6 으로 갈렸고, 가짜 DB 는 정석 수정이 보낸
  질의에 **0 / 1** 로 답을 못 줬다. 스냅숏은 행과 카운터를 같이 되돌려 **글자까지 같은 응답**을 냈다.
  🔴 고른 이유는 속도가 아니라 **되돌릴 목록을 몰라도 된다**는 것이다.
  🧭 [reports/2026-09-11/db-restore-experiment.md](docs/reports/2026-09-11/db-restore-experiment.md)

## v0 — 고리 전체 (3~4주 · 바이트코드 없음)

### 1주차
- ✅ `git init`
- ✅ Gradle 빌드가 실제로 도는지 확인. 버전 실재 확인 (foojay는 0.9.0→1.0.0으로 고쳐야 했다)
- ✅ `hindsight-model` — 기록 자료 구조. Jackson 애너테이션 없이
- ✅ `hindsight-core` — 기록 읽기·쓰기 + **왕복 테스트**
- ✅ 기록 표본 (전부 채운 것 / 「모름」이 많은 것) — `hindsight-model` 의 테스트 픽스처로
- ✅ 🔄 모듈을 13개에서 6개로 접었다 ([module-boundary-decision.md](docs/rules/module-boundary-decision.md))
- ✅ 위 「먼저 확인해야 할 것」 — **넷 다 실측으로 확인**했다 (2026-09-11)
- 🔄 `demo-app` — **최소 뼈대는 섰다** (엔티티 2 · N+1 심음 · H2 메모리 · 서버 불필요).
  ⬜ 🔴 **아직 「진짜 서비스」가 아니다** — 페이징·Redis·Docker Compose·OpenAPI 가 없다.
  평가할 수 없는 포트폴리오는 0점이므로 이건 남은 과제다 (설계 §3-4)

### 2주차
- ✅ **v0 기록기** — `Filter` + `DataSource` 감싸기. 진짜 앱에 붙여 한 바퀴 돌았다
  ([v0-recorder.md](docs/reports/2026-09-15/v0-recorder.md))
  - ✅ 🎯 **「따로 모듈인가 패키지인가」를 재서 끝냈다** — 모듈. demo-app 의 외부 의존성이
    붙이기 전후로 **59→59**, 늘어난 것이 0 이었다
    ([module-boundary-decision.md](docs/rules/module-boundary-decision.md) §6-2)
  - ✅ 방아쇠 둘(예외 · 지연) · 포획 폭주 막기 · 재진입 막기 · 예외 봉쇄 · 자기 무력화
  - ✅ 🔴 **「모름」을 살려서 적는다** — 결과 행 · 행 수 · 커넥션 표본 · JFR · 재생 정보가
    전부 `null` 이고, 그게 「안 봤다」라는 뜻이다
- ✅ 가명화 — HMAC, 🔴 **파일로 쓰기 직전 한 곳에서만**.
  쓰기와 «같은 함수 안»에 묶어서 건너뛰는 경로가 없게 했다 (`core.store.RecordingStore`)
- ✅ 🔴 **성능 측정 하네스** — 쟀다. 목표 넷 중 **둘 지킴 · 하나는 못 지켰다가 고쳐서 지킴 ·
  하나는 못 쟀음** ([recorder-overhead.md](docs/reports/2026-09-15/recorder-overhead.md))
  - 🎯 **SQL 이벤트 5.42µs → 0.46µs** (목표 2µs). 비싼 자리를 «재서» 찾았다 —
    링 버퍼가 아니라 요약층의 모양 만들기(정규식 3개)가 62% 였다
  - 🔴 **사고 때 파일 쓰기 12ms.** 평상시 비용은 아니지만 **요청 스레드가 기다린다**
  - ⬜ **요청당 추가 시간은 「못 쟀다」** — 재려는 값(수십 µs)이 스프링 요청의 잡음
    (2,326µs)보다 작다. 🔴 「차이가 없다」가 아니라 「이 자로는 안 보인다」
  - ⬜ **앱 시작 시간 증가도 못 쟀다** — 같은 JVM 에서 두 번째로 뜨는 앱은 유리해서
    비교가 성립하지 않는다. JVM 을 따로 띄워야 한다
- 🔄 **이벤트 크기 「어림」이 실제와 얼마나 다른가** — 쟀다. **실제 / 어림 = 0.38 배**
  (5만 건에 어림 20.9MB, 실제 힙 7.9MB). 과대평가라 안전한 방향이지만 공짜가 아니다 —
  ⬜ 32MB 상한이 실제로는 12MB 남짓만 담아서 **담기는 시간이 그만큼 짧아진다**
- ⬜ 🔴 **동시 요청에서 재기** — 지금까지 전부 한 스레드로 쟀다. 여러 스레드가 같은 링 버퍼를
  두드릴 때 잠금 다툼이 어떻게 되는지는 **안 봤다**
- ⬜ `hs list` / `hs show` — 🔴 `windowFellShort()` 일 때 눈에 띄게 경고할 것

### 3주차
- ✅ 🔴 **오라클 셋** — 예외 / 질의 모양 반복 / 응답 값.
  🔴 예외 방아쇠에도 **응답 대조가 항상 낀다**(예외만 보면 삼키는 패치가 통과한다).
  🔴 지연은 **시간이 아니라 횟수**로 잡는다(재생에서는 시간이 사라진다)
  - ✅ 🔴 **결과가 넷이다** — 통과 · 실패 · 갈라짐 · **판정 못 함**.
    「판정 못 함」을 「통과」로 접지 않는다
- ✅ 🔴 **`DIVERGED`** — 기록에 없는 질의를 만났을 때. 「무엇이 무엇으로 바뀌었나」(20→1)를
  붙여 사람에게 넘긴다. 🔴 자동 PR 로는 «안» 간다
- ✅ **등급 판정** — 재생이 안 돌았나 · 갈라졌나 · 못 되돌렸나 · 기록에 구멍이 있었나.
  🔴 전부 **순수 함수**라 JVM 을 띄우지 않고 검사된다
- ✅ 🔄 **질의 지문을 한 자리로 모았다** (`hindsight-model`).
  🔴 두 벌이었으면 **모든 재생이 채점 불가가 되면서도 오류가 안 났을 것**이다.
  일부러 어긋나게 해서 검사가 막는 것을 확인했다
- ⬜ 🔴 **재생 엔진** — 기록을 실제로 앱에 다시 흘려 넣기. **여기가 다음이다**
  - ⬜ 🔴 **복원 단계** — 스냅숏을 떠 두고 재생 전에 되돌린다. 못 되돌렸으면 등급 `PARTIAL` ·
    `missing: ["STATE"]` ([db-restore-decision.md](docs/rules/db-restore-decision.md))
  - ⬜ **아웃바운드 가짜** — v0 기록기가 나간 요청을 «안 잡으므로» 잡을 것이 생긴 뒤에 한다
- ⬜ JUnit 테스트 생성 — 채점 기준이 굳었으니 이제 코드로 뽑아낼 수 있다
- ⬜ 🔴 **질의 모양 접기가 진짜 앱에서 얼마나 듣는지** — `in (1,2)` 와 `in (1,2,3)` 이
  **다른 모양으로 세인다**는 것을 검사로 확인했다. 배치 크기가 들쭉날쭉한 앱에서는
  N+1 을 놓칠 수 있는데, **얼마나 흔한지는 안 쟀다**

### 4주차
- ⬜ `hindsight-guard` — 경로 화이트리스트. 🔴 **파일을 쓰는 유일한 통로로 만든다**
- ⬜ `hindsight-brain` — 진단 + 🔴 **채점 네 겹** (기준선 실패 · 통과 · 기존 테스트 · 되돌리기)
- ⬜ PR 제출
- ✅ ~~성능 측정 하네스~~ — **2주차로 당겨서 했다.** 기록기가 생긴 «직후»가 잴 수 있는
  가장 이른 시점이었고, 미루면 안 재게 되기 때문이다
  ([recorder-overhead.md](docs/reports/2026-09-15/recorder-overhead.md))

### v0 완료 조건
사람 손 없이 **PR 하나**가 올라오고, 네 겹 채점을 다 통과한다. 그리고 **3분짜리 영상**이 찍힌다.

---

## v1 — 진짜 에이전트 (4~6주)
- ⬜ `hindsight-agent-boot` — 🔴 **부트스트랩 껍데기.** 없으면 JDK 클래스 계측이 JVM을 못 뜨게 한다
- ⬜ `premain` · ByteBuddy · 셰이딩 · 격리 클래스로더
- ⬜ `DispatcherServlet#doService` 로 잡기 (🔴 `Filter` 인터페이스로 잡지 않는다)
- ⬜ JDBC — 🔴 **커넥션 풀 프록시와 드라이버가 둘 다 잡히지 않게**
- ⬜ 시각·난수 계측 · 재생 등급 실측 (설계 §6-3)
- ⬜ 🔴 **jar 검사** — 완성된 jar에 `net.bytebuddy` 패키지가 없는지. 값이 가장 큰 테스트 한 개
- ⬜ 🔴 **v0 기록기와 같은 기록이 나오는지 대조** — 이게 회귀 검사다
- ⬜ CI에서 demo-app을 **Java 17·21·25 셋 다** 돌린다

---

## v2 — 넓히기 (2~3주)
- ⬜ JFR 연동 · 요약 층 · 자원/메모리 방아쇠
- ⬜ 🔴 **오픈소스 스프링 프로젝트의 진짜 이슈 3건**으로 시험 (설계 §11-1).
  심은 버그 20건보다 진짜 3건이 낫다

## v3 — 열어주기 (2주)
- ⬜ `hindsight-mcp` · `hindsight-server`

---

## ⬜ 아직 결정 안 한 것 — 코드가 먼저 정하지 않게

- ✅ ~~상태를 바꾸는 요청(POST 등)을 어떻게 채점하나~~ — **2026-09-11 풀렸다.**
  질문 자체가 틀린 축이었다. 쓰기라서 다른 게 아니라 **되돌리지 않아서** 달랐다.
  등급은 넷 그대로 두고, 재생의 정의에 **복원**을 넣었다.
  🧭 [rules/replay-state-decision.md](docs/rules/replay-state-decision.md)

- ✅ ~~진짜 서비스의 DB 를 기록 시점으로 어떻게 되돌리나~~ — **2026-09-11 풀렸다.**
  후보 셋을 갈라서 재고 **스냅숏**으로 정했다. 롤백은 카운터를 안 되돌리고, 가짜 DB 는
  정석 수정을 채점 못 한다. 🧭 [rules/db-restore-decision.md](docs/rules/db-restore-decision.md)
  ⬜ **남은 것 하나**: 운영 규모에서 스냅숏 복원이 몇 초 걸리나. v0 는 재생 환경이 H2 라
  문제가 없고, v1 에서 진짜 DB 를 쓸 때 잰다
- ✅ ~~「행으로 안 보이는 상태」가 또 무엇이 있나~~ — **2026-09-11 전수로 세었다.**
  H2 에게 물어(`INFORMATION_SCHEMA` 목록 35개가 분모) 값을 들고 있을 수 있는 객체를 전부 세니
  **행 밖의 객체는 0개**, 복원 목록은 **둘**(행 · 자동 증가 카운터)이다. 다섯을 안 넘었다.
  🔴 **새로 찾은 것**: H2 는 자동 증가 값을 세션에게 32개씩 미리 준다(`IDENTITY_CACHE = 32`).
  ⬜ **영향은 안 재었다** — 세션 여러 개로 재야 한다. 「보았지만 모른다」로 목록에 올려 둔다

- ✅ ~~v0 기록기를 따로 모듈로 둘 것인가~~ — **2026-09-15 재서 「모듈」로 정했다.**
  미룬 이유였던 「`core` 를 의존하면 Spring AI 와 picocli 가 딸려 온다」가 **오늘은 참이 아니었다** —
  demo-app 의 외부 의존성이 **59→59**, 늘어난 것이 0 이다. `core` 가 지금 쓰는 것은
  Jackson 2.18.2 뿐이고 스프링이 이미 같은 버전을 들고 있다.
  🔴 그 걱정이 «참이 되는 날»은 `checkRecorderDependencies` 가 빌드를 세워서 알려 준다
  🧭 [module-boundary-decision.md](docs/rules/module-boundary-decision.md) §6-2
- **에이전트의 직렬화 형식**: 손으로 짠 JSON이냐 이진 프레임이냐.
  리뷰는 이진을 권했다(이진 본문·짝 없는 서로게이트에서 JSON이 깨진다).
  v0에는 손으로 짠 기록기가 없으므로 **v1에서 정한다**
- 🔴 **시각·난수 계측을 포기할 것인가** — 포기하면 `hindsight-agent-boot` 모듈이
  **통째로 사라진다**(부트스트랩 클래스로더가 필요한 유일한 이유라서). v1 에서 가장 위험한
  부분이 없어진다. 대가: 재생 등급이 **항상 `PARTIAL`** 이 되어 「얼마나 믿을 만한지」를 못 잰다.
  ⚠️ 그런데 그 등급을 재는 것이 Keploy 같은 도구 대비 우리의 유일한 우위다 (PRD §7).
  **v1 착수 전에 정한다**
- 🔴 **`hindsight-model` 을 아예 없앨 것인가** — v1 에이전트는 어차피 JSON 을 손으로 쓰므로
  모델 클래스가 꼭 필요하진 않다. 없애면 모듈이 하나 줄고 에이전트가 진짜로 자립한다.
  대가: **기록 형식이 두 벌이 되어 조용히 갈라질 수 있다** — 컴파일러가 안 잡아 주고
  골든 파일 검사로만 맞춰야 한다. 지금은 안 없애는 쪽이 낫다고 보지만 **선택지이지 불가능이 아니다**
- **기록 보관 기간**: 지금은 총량 1GB 상한만 있다
- **기록 저장소**: 파일 → DB 전환 시점. v3에서 화면이 필요해지면

## ⏸ 미루기로 한 것 (설계에 적힌 이유가 있다)
- 창 전체(여러 요청) 재생 — v0·v1은 요청 하나만
- 격리 상자에서 검증 실행 — 대신 환경변수를 씻는다
- 분산 재현 · 차등 재생 — 범위 밖
- GitHub Releases 배포 — v3 이후

---

## 🔴 리뷰가 지적했지만 아직 코드로 지켜지지 않는 것

⚠️ **문서만 고쳤다. 검사는 하나도 없다.** 각각을 지키는 테스트가 생길 때 여기서 지운다.

- 🔄 왕복 테스트 (기록 형식이 두 벌로 갈라지지 않는지) — **한 겹 깊어졌다.**
  이제 **기록기가 만든 파일을 core 가 읽는다** (실험 ⑨ 포함).
  🔴 다만 아직 코덱이 한 벌이라 «진짜» 위험(쓰는 쪽과 읽는 쪽이 두 벌)은
  v1 에이전트가 손으로 JSON 을 쓸 때 비로소 검사된다
- ✅ 의존성 금지선이 실제로 빌드를 막는지 (2026-09-10 확인)
- ✅ **문서 링크 검사가 실제로 빌드를 막는지** (2026-09-11 확인) — `checkDocLinks`,
  `check` 에 걸려 있다 ([doc-layout-decision.md](docs/rules/doc-layout-decision.md) §5)
- ⬜ 화이트리스트가 `src/test/**` 패치를 실제로 거부하는지 (LLM을 가짜로 갈아끼우고)
- ⬜ 골든 파일 테스트 (제어 문자 · 짝 없는 서로게이트 · `NaN` · 이진 본문)
- 🔄 재생 결정론 (같은 기록을 두 번 재생하면 같은가) — 🔴 **깨지는 조건을 하나 찾았다.**
  복원을 트랜잭션 롤백으로 하면 두 번의 재생이 `id` 5·6 으로 갈린다
  ([실측](docs/reports/2026-09-11/db-restore-experiment.md)). 재생기가 생기면 이걸 검사로 만든다
- 🔄 **기록기 예외가 밖으로 안 나가는지** — ✅ v0 기록기는 검사가 생겼다(쓰기 실패 · SQL 기록
  실패 · 예외 종류 안 바뀜). ⬜ v1 에이전트는 아직
- ✅ **담긴 시간과 담으려던 시간이 실제로 둘 다 기록되는지** — 단위 검사와 실험 ⑨ 둘 다 본다
- ✅ **기록 파일에 개인정보가 없는지 검사하는 테스트** — 가명화 10건 + 실험 ⑨ 에서
  진짜 요청의 인증 헤더가 파일에 안 남는 것을 본다
- 🔄 **이벤트가 순서대로 들어가는지** — 순서가 섞이던 결함을 2026-09-15 에 잡아 검사로 만들었다.
  🔴 다만 이건 **링에 넣는 순서**만 본다. 재생이 그 순서를 지키는지는 재생기가 생겨야 안다
- ⬜ 🔄 **프롬프트 캐싱이 실제로 듣는지 검사** — 응답의 `cache_read_input_tokens` 가 0 이면
  캐시가 매번 깨지고 있다는 뜻인데, 값이 통째로 사라지면서 **알아채기가 어렵다.**
  재시도 구조라 캐싱 여부가 비용을 40% 대 100% 로 가른다 (설계 §7-4)
- ⬜ 🔄 **패키지 의존 방향 검사** — 「`replay` 가 `brain` 을 몰라야 한다」,
  「`brain` 이 `java.nio` 를 직접 부르면 안 된다」. 모듈로 나눠서 막으려 했던 것을
  검사로 옮긴 자리다 ([module-boundary-decision.md](docs/rules/module-boundary-decision.md) §4).
  🔴 지금은 **사람이 지킨다** — 즉 안 지켜질 수 있다
