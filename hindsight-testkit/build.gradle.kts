// 여러 모듈이 함께 쓰는 기록 표본과 테스트 도우미.
//
// 🔴 표본이 한 곳에만 있어야 하는 이유: 모듈마다 자기 표본을 만들면, 기록 형식이 바뀔 때
// 어떤 모듈은 고치고 어떤 모듈은 안 고친 채로 전부 초록색이 된다. 갈라진 걸 아무도 모른다.

dependencies {
    api(project(":hindsight-model"))
}
