# 코드 개관 — 어디에 무엇이 있고, 왜 그 모듈이 있나

> **최종 갱신: 2026-09-10**
>
> 🔴 **아직 한 바퀴가 안 돈다.** 기록을 담는 그릇과 그 그릇을 읽고 쓰는 코드까지 왔다.
> 기록을 «만드는» 코드도, 재생하는 코드도, 진단하는 코드도 아직 없다.
>
> | | 지금 |
> | --- | --- |
> | 모듈 | **3개** 있다 (계획은 13개 — §2) |
> | 소스 | 자바 **12개** (본체 11 + 검사 1) |
> | 검사 | **9건** 통과 |
> | 빌드 | Gradle 9.3 · 이중 툴체인 **실측 확인** — model major=61(Java 17), core major=65(Java 21) |
> | 돌려 본 것 | 왕복 · 「모름」 보존 · 판 번호 거부 · 자동 PR 조건 · 의존성 금지선 |
>
> ⚠️ **「검사 9건 통과」는 「맞다」가 아니다.** 지금 왕복 검사는 **core 가 쓰고 core 가 읽는**
> 왕복이다. 진짜 위험 — 기록을 «만드는» 쪽과 «읽는» 쪽이 두 벌인 것 — 은
> `hindsight-recorder-simple` 이 생겨야 비로소 검사된다.
>
> 코드가 생길 때마다 이 문서를 **같은 커밋에서** 갱신한다 — 모듈 지도, 파일별 역할,
> 그리고 위의 「최종 갱신」 날짜까지.

---

## §0. 이 문서를 처음 보는 사람에게

| 말 | 뜻 |
| --- | --- |
| **에이전트(agent)** | 관측 대상 앱과 **같은 JVM 안에서** 도는 우리 코드. `-javaagent` 옵션으로 붙는다 |
| **계측(instrumentation)** | 원본 소스를 안 고치고 실행 시점에 코드를 끼워 넣는 것 |
| **경계(boundary)** | 프로세스 밖과 값이 오가는 자리. HTTP 요청·응답, DB 질의, 시각, 난수 |
| **기록(recording)** | 사고 순간 앞뒤의 경계 값을 묶은 파일 하나 |
| **재생(replay)** | 저장한 기록을 다시 흘려 넣어 그때 일을 한 번 더 일으키는 것 |
| **오라클(oracle)** | 테스트가 「맞았다/틀렸다」를 판정하는 기준. **입력만으로는 테스트가 아니다** |
| **셰이딩(shading)** | 의존 라이브러리를 내 패키지 이름 안으로 옮겨 남의 것과 안 부딪히게 하는 것 |
| **툴체인(toolchain)** | 이 모듈을 어느 자바 판으로 컴파일할지. 모듈마다 다르게 줄 수 있다 |
| **믹스인(mixin)** | 원본 클래스를 안 고치고 「이 클래스는 이렇게 읽어라」를 밖에서 알려주는 방식 |

---

## §1. 전체 흐름 — 이 도구가 하는 일

한 줄로 이렇다.

```
자바 앱이 사고 나면 → 그 순간을 기록해 → 재생해 실패 테스트를 만들고 →
LLM 이 고치면 → 재생으로 걸러낸 뒤 → PR 을 올린다.   배포는 사람이 한다.
```

여섯 마디이고, 각 마디가 모듈 하나에 대응한다.

```
   [관측 대상 앱의 JVM 안]              │        [바깥]
                                        │
   demo-app                             │
      │  요청이 들어오고 SQL 이 나간다   │
      ▼                                 │
   recorder-simple  (v0)                │
   agent            (v1)                │  ← 같은 일을 하는 두 벌.
      │  경계 값을 링 버퍼에 담는다      │     v1 이 v0 을 대체한다 (§2-1)
      │                                 │
      │  사고! (예외 · 지연)             │
      ▼                                 │
   ┌──────────────┐                     │
   │ 기록 파일     │ ────────────────────┼──▶  core       파일을 읽는다
   │ recording.json                     │       │        「모름」을 살려서
   └──────────────┘                     │       ▼
        ▲                               │     replay     다시 흘려 넣는다
        │ model 이 이 파일의 «모양»을 정한다     │        + 🔴 오라클 (무엇을 검사하나)
        │ 안쪽과 바깥쪽이 같은 model 을 쓴다     ▼
                                        │     brain      LLM 진단 → 패치
                                        │       │        ↕ 네 겹 채점
                                        │       ▼
                                        │     guard      🔴 패치가 건드려도 되는 경로인가
                                        │       │        파일을 쓰는 유일한 통로
                                        │       ▼
                                        │      PR        사람이 머지한다
```

🔴 **`model` 이 경계선 위에 걸쳐 있는 것이 이 구조의 핵심이다.** 앱 «안»에서 도는 코드와
«밖»에서 도는 코드가 **같은 자료 구조**를 써야 기록이 오간다. 그런데 안쪽은 남의 클래스패스라
의존성을 못 늘린다. 그래서 `model` 만 따로 떼어 **의존성 0, Java 17** 로 묶어 뒀다.

---

## §2. 모듈 지도 — 🔴 13개 중 3개만 있다

> ⚠️ **모듈 폴더는 첫 파일을 쓸 때 그때 만든다.** 빈 폴더를 미리 만들어 두지 않는다.
> 빈 폴더는 「누가 시작해 놨나」로 읽혀서, 다음 사람이 매번 열어 보고 비었다는 것을 확인해야 한다.
> 없으면 그 질문 자체가 안 생긴다.
> (2026-09-10 에 실제로 이 실수를 했다 — 13개를 미리 만들어 56개의 빈 폴더가 생겼고,
> 「이게 다 뭐냐」는 질문을 받고서야 지웠다.)
>
> 🧭 **그래도 이 표는 필요하다.** 「무엇을 어디에 둘 것인가」를 첫 파일을 쓰는 순간에 즉흥으로
> 정하면, 재생(`replay`)과 진단(`brain`)이 한 파일에 섞이는 식으로 무너진다.

### 남의 JVM 안에 들어가는 것

| 모듈 | 있나 | 자바 | 역할 | 🧭 왜 따로 두나 |
| --- | --- | --- | --- | --- |
| `hindsight-model` | ✅ **있다** | **17** | 기록 파일의 자료 구조. record 뿐 | 🔴 **안쪽과 바깥쪽이 같이 쓰는 유일한 것.** 그래서 의존성이 0이어야 한다 |
| `hindsight-agent-boot` | ⬜ v1 | **17** | 부트스트랩 클래스로더에 올라가는 얇은 껍데기 (100줄 남짓) | 🔴 **없으면 JVM 이 아예 안 뜬다.** `java.time.Instant` 에 끼워 넣은 코드는 부트스트랩에서 도는데, 거기서 시스템 클래스로더의 우리 클래스를 못 찾으면 `NoClassDefFoundError` 다 |
| `hindsight-agent` | ⬜ v1 | **17** | `premain` · 계측 · 링 버퍼 · 방아쇠 · 덤프 | 남의 앱 안에서 도는 모든 것. 의존성은 ByteBuddy 하나뿐이고 그것도 셰이딩한다 |

🔴 **이 셋만 Java 17 이다.** 실무에 Java 17 앱이 여전히 더 많고, 에이전트는 붙을 수 있는 앱이
많을수록 쓸모가 커진다. 나머지는 21. 근거: [`rules/stack-decision.md`](rules/stack-decision.md)

### 바깥에서 도는 것

| 모듈 | 있나 | 역할 | 🧭 왜 따로 두나 |
| --- | --- | --- | --- |
| `hindsight-core` | ✅ **있다** | 기록 읽기·쓰기, 판 번호 검사, (예정) 가명화 | 🔴 **Jackson 이 여기까지만 온다.** model 에 넣으면 남의 JVM 으로 딸려 들어간다 |
| `hindsight-testkit` | ✅ **있다** | 기록 표본과 테스트 도우미 | 🔴 **표본이 한 곳에만 있어야 한다.** 모듈마다 자기 표본을 만들면 형식이 바뀔 때 일부만 고치고 전부 초록색이 된다 |
| `hindsight-recorder-simple` | ⬜ **v0 다음** | v0 의 기록기. `Filter` + `DataSource` 감싸기 | 🔴 **바이트코드를 안 쓴다.** 200줄이고 오늘 된다. 위험을 뒤로 미루는 자리 ([`rules/build-order-decision.md`](rules/build-order-decision.md)) |
| `hindsight-replay` | ⬜ v0 | 재생 · **오라클** · JUnit 테스트 생성 | 🔴 오라클(무엇을 검사하나)이 이 프로젝트에서 가장 늦게 발견된 구멍이다. 재생과 같은 자리에 둔다 — 「입력만 재현하고 판정을 안 정하는」 실수를 구조로 막으려고 |
| `hindsight-guard` | ⬜ v0 | 패치가 건드려도 되는 경로인지 검사 | 🔴 **파일을 쓰는 유일한 통로.** 다른 모듈 안에 숨겨 두면 우회하는 데 호출 한 줄이면 된다. 자기 모듈이면 우회하려고 이상한 것을 import 해야 한다 |
| `hindsight-brain` | ⬜ v0 | LLM 진단 · 네 겹 채점 · PR 제출 | LLM 관련 의존성(Spring AI)을 여기 가둔다 |
| `hindsight-cli` | ⬜ v0 | `hs` 명령어 | 사람이 만지는 표면. 🔴 담긴 시간이 짧으면 **눈에 띄게 경고**해야 하는 자리 |
| `demo-app` | ⬜ **v0 우선** | 관측 대상 | 🔴 **소품이 아니라 진짜 스프링 서비스다** (§2-2) |
| `hindsight-mcp` | ⬜ v3 | 코딩 에이전트가 기록을 읽고 재생하게 여는 통로 | — |
| `hindsight-server` | ⬜ v3 | 저장소 · REST · 화면 | 🔴 `replay` 에도 의존한다 — v3 의 목표가 *"재생해봐"* 라서 |

### §2-1 🔴 기록기가 왜 두 벌인가 (`recorder-simple` 과 `agent`)

같은 일을 하는 모듈이 둘이라 이상해 보인다. 일부러 그렇게 했다.

```
v0   recorder-simple    Filter + DataSource 감싸기      200줄, 오늘 된다
      └─ 소스를 고쳐야 한다. 아무 앱에나 못 붙는다
v1   agent              premain + ByteBuddy + 셰이딩    4~6주, 위험하다
      └─ 옵션 한 줄로 아무 앱에나 붙는다
```

처음 계획은 `agent` 부터 만드는 것이었다. 뒤집은 이유:

> 🔴 **일정이 밀리면 잘려나가는 건 항상 뒤쪽이다.** 그런데 뒤쪽에 이 프로젝트의 존재 이유
> (채점 고리)가 있었다. **잘릴 가능성이 가장 큰 것이 하필 핵심**이었다.

`recorder-simple` 로 먼저 고리를 한 바퀴 돌리면 **3주 뒤에 데모 영상이 나온다.**
그다음 기록기만 갈아끼운다. 이때 **이미 도는 시스템이 회귀 검사 역할을 한다** — 같은 상황에서
두 기록기가 같은 기록을 만드는지 비교하면 된다.

⚠️ **버리는 값은 나흘쯤이다.** 그 대신 「위험보다 데모가 먼저 온다」를 산다.
🧭 근거: [`rules/build-order-decision.md`](rules/build-order-decision.md)

### §2-2 🔴 `demo-app` 은 왜 「진짜」여야 하나

`demo-app` 은 버그를 심을 대상이니 대충 만들어도 될 것 같다. 그런데:

> 이 프로젝트를 다 끝내도 **스프링 코드를 거의 안 쓴 상태**가 된다. 자바·스프링 자리에
> 지원하면서. 그리고 **바이트코드 계측을 평가할 수 있는 면접관은 소수다.**
> ⚠️ 평가할 수 없는 포트폴리오는 높은 점수가 아니라 **0점**을 받는다.

그래서 JPA 연관관계·트랜잭션·페이징·Redis·Docker Compose·CI 를 갖춘 서비스로 만든다.
포트폴리오가 두 겹이 된다 — *"스프링 서비스를 만들었고, 그걸 디버깅하는 JVM 에이전트도 만들었다."*
앞쪽은 누구나 평가할 수 있고 뒤쪽은 아는 사람이 알아본다.

---

## §3. 지금 있는 파일 12개

### `hindsight-model/` — 기록의 모양 (자바 8개)

🔴 **이 모듈에는 애너테이션이 하나도 없다.** Jackson 애너테이션을 붙이는 순간
`jackson-annotations` 가 이 모듈의 의존성이 되고, 에이전트가 이 모듈을 쓰므로 **Jackson 이
관측 대상 앱의 클래스패스로 딸려 들어간다.** 앱이 다른 버전을 쓰고 있으면 우리가 아니라 그 앱이 죽는다.

| 파일 | 무엇 |
| --- | --- |
| `Recording.java` | 최상위. 파일 하나 = 이것 하나. `CURRENT_SCHEMA_VERSION` 도 여기 |
| `Event.java` | 경계에서 오간 한 건. **`sealed interface`** 에 다섯 종류가 안에 들어 있다 |
| `Trigger.java` | 무엇이 이 기록을 만들었나. 묶는 열쇠 만드는 법도 여기 |
| `AppInfo.java` | 어떤 코드에서 났나 (커밋 · 자바 판 · 호스트) |
| `Summary.java` | **요약 층.** 전문보다 훨씬 앞선 시간까지 담는다 |
| `JfrSummary.java` | JVM 이 스스로 남긴 지표에서 뽑은 요약 (v2) |
| `ReplayInfo.java` | **이 기록을 얼마나 믿을 수 있나.** 등급과 자동 PR 조건 |
| `Integrity.java` | **무엇을 못 담았나.** 이 프로젝트의 존재 이유를 도구 자신에게 적용하는 자리 |

#### 🔴 `Event` 를 `sealed` 로 닫은 이유

재생기와 오라클이 이벤트 종류별로 갈라져 처리한다. 새 종류가 생겼을 때 처리를 빠뜨리면
**컴파일러가 잡아 준다.** 열려 있으면 조용히 무시되고, 무시된 이벤트가 재생에서 빠진다.

#### 🔴 `Integrity` 가 이 프로젝트에서 가장 중요한 record 다

이 도구는 남의 코드에서 「모름을 없음으로 접는」 결함을 잡으려는 물건이다.
그런데 **처음 설계가 자기 자신에게 그 결함을 저지르고 있었다.**

```
설정에 60초라고 써 있다 → 트래픽이 많으면 실제로는 4초치만 담긴다
                        → 그런데 파일에는 「60초」라고 적힌다
                        → 읽는 사람은 30초 전에 시작된 커넥션 누수가 «없었다»고 결론 낸다
```

그래서 `windowRequestedSeconds`(담으려 한 시간)와 `windowActualSeconds`(실제 담긴 시간)를
**둘 다** 적는다. 그리고 `droppedEvents`(아예 못 받음)와 `evictedEvents`(받았다가 밀어냄)도
**합치지 않는다** — 다른 사실이라서.

#### 🔴 파생 메서드는 `get`·`is` 로 시작하지 않는다

`hasGaps()`, `windowFellShort()`, `allowsAutoPullRequest()` — 전부 이 규칙 때문이다.

애너테이션을 못 붙이니 `@JsonIgnore` 로 막을 수가 없고, `isXxx()` 로 지으면 Jackson 이
**자동으로 JSON 필드로 만들어** 읽을 때 왕복이 깨진다. 2026-09-10 에 `isIncomplete()` 가
실제로 이걸 일으켰다.

⚠️ **매퍼에서 getter 인식을 끄는 방법은 못 쓴다** — Jackson 은 record 접근자도 getter 로 봐서
전부 꺼지고 `{ }` 가 나온다. 시도했다가 되돌렸다.

### `hindsight-core/` — 기록을 읽고 쓴다 (자바 3개)

| 파일 | 무엇 |
| --- | --- |
| `store/RecordingCodec.java` | 읽기·쓰기. 판 번호 검사도 여기 |
| `store/EventMixin.java` | `Event` 종류를 JSON 에 어떻게 적을지를 **밖에서** 알려준다 |
| `store/RecordingCodecTest.java` | 검사 9건 |

#### 🔴 `RecordingCodec` 이 지키는 것 셋

**① `null` 을 생략하지 않는다.** 흔히 `NON_NULL` 로 빈 필드를 지우는데, 여기서는 일부러 안 한다.
`null` 이 「안 봤다/못 봤다」라는 **내용 있는 값**이라서, 지우면 파일만 봤을 때
「못 알아냈다」와 「그런 게 없었다」가 똑같아 보인다.

**② 모르는 판 번호는 거부한다.** 기본값으로 대충 읽지 않는다. 잘못 읽은 재생 결과가
그대로 LLM 진단의 근거가 되기 때문이다. 그리고 **내용을 보기 전에 판 번호부터** 본다 —
순서가 바뀌면 실패 메시지가 「필드 이름이 이상하다」로 나와서 진짜 원인이 가려진다.

**③ 모르는 필드가 있으면 멈춘다.** 조용히 버리면 쓰는 쪽과 읽는 쪽이 갈라진 것을
몇 주 동안 아무도 모른다.

#### `EventMixin` — 클래스 이름을 JSON 에 안 적는 이유

Jackson 에게 종류를 알려주는 흔한 방법은 클래스 이름을 적는 것(`@class`)인데, 그러면
**기록 파일이 우리 패키지 구조에 묶인다.** 나중에 클래스를 옮기면 옛 기록을 못 읽는다.
기록은 오래 남아야 하므로 `HTTP_IN`·`SQL` 같은 **이름을 직접 정해 고정**한다.

### `hindsight-testkit/` — 표본 (자바 1개)

`Recordings.java` 가 표본 둘을 준다.

- **`full()`** — 모든 자리가 채워진 기록. N+1 상황을 본떴다. 「빠뜨린 필드가 없나」를 본다
- **`mostlyUnknown()`** — 알아낸 게 거의 없는 기록. 🔴 **「모름」이 살아서 왕복하는지**를 본다.
  커밋을 못 알아냈고, JFR 을 못 떴고, 재생을 아직 안 돌려봤다. 이 값들이 왕복 뒤에
  빈 목록이나 `false` 로 바뀌어 있으면 그게 결함이다

🔴 **여기 값은 전부 합성이다.** 진짜 트래픽에서 뜬 기록은 저장소에 올리지 않는다 —
가명화를 거쳤더라도 형태와 흐름이 남고, **가명정보는 여전히 개인정보다.**

### 빌드 파일 — `build.gradle.kts` 의 `checkAgentDependencies`

🔴 **이 프로젝트에서 문서가 아니라 «빌드»가 지키는 규칙이 하나 있다.**

에이전트 계열 모듈에 허용 목록(`byte-buddy`, `hindsight-model`) 밖의 의존성이 들어오면
**빌드가 실패한다.** 문서에만 「의존성을 늘리지 마라」라고 적어두면 언젠가 누가 편하다는 이유로
한 줄 추가하고, 그 순간 남의 클래스패스가 조용히 오염된다.

✅ **실제로 막는 것을 확인했다** (2026-09-10) — `hindsight-model` 에 Jackson 을 넣으니
빌드가 실패했고, 빼니 성공했다. **통과만 하는 검사는 검사가 아니다.**

---

## §4. 모듈 ↔ 근거 표

「왜 이 모듈이 이렇게 생겼나」의 답이 있는 곳.

| 모듈·결정 | 근거 |
| --- | --- |
| 에이전트 의존성 0, 셰이딩, 예외 봉쇄, 자기 무력화 | [`rules/agent-safety-decision.md`](rules/agent-safety-decision.md) |
| 바이트코드를 v1 로 미룬 것, 기록기가 두 벌인 것 | [`rules/build-order-decision.md`](rules/build-order-decision.md) |
| 자바 21/17 이중 툴체인, 25 를 안 쓴 이유, 파생 메서드 이름 규칙 | [`rules/stack-decision.md`](rules/stack-decision.md) |
| 기록 파일의 필드 하나하나, 「모름」의 표현, 상한값 | [`DATA_CONTRACT.md`](DATA_CONTRACT.md) |
| 단계 · 오라클 · 채점 네 겹 · 개인정보 | [`DESIGN.md`](DESIGN.md) |
| 설계 리뷰에서 무엇이 무너졌고 무엇을 고쳤나 | [`review/2026-09-10-design-review.md`](review/2026-09-10-design-review.md) |
| 지금 어디까지 왔나, 다음에 뭘 하나 | [`../TODOS.md`](../TODOS.md) |

---

## §5. 실행

🔴 **이 기계에는 JDK 17 하나만 있다.** Gradle 이 필요한 판을 직접 받아 오지만,
래퍼를 띄울 자바는 손으로 알려줘야 한다.

```powershell
$env:JAVA_HOME = "C:\Users\USER\.gradle\jdks\eclipse_adoptium-17-amd64-windows.2"
cd C:\intellij_workspace\hindsight_project

.\gradlew.bat build                        # 전체
.\gradlew.bat :hindsight-core:test         # 만진 모듈만
.\gradlew.bat :hindsight-model:checkAgentDependencies   # 의존성 금지선
```

⚠️ **PowerShell 콘솔에서 한글 테스트 이름이 깨져 보인다.** 실패 원인을 볼 때는 콘솔 대신
결과 파일을 본다.

```bash
grep -o 'message="[^"]\{0,220\}"' hindsight-core/build/test-results/test/*.xml
```

⚠️ **PowerShell 로 소스 파일을 왕복시키지 말 것** (`Get-Content` → `Set-Content`).
한글 주석이 통째로 깨진다. 2026-09-10 에 실제로 겪었고 편집 도구로 복구해야 했다.

### 아직 없는 명령

`hs list` · `hs show` · `hs replay` · `hs diagnose` · `hs submit` 은 **아직 없다.**
`hindsight-cli` 가 안 생겼다 (§2).

---

## §6. 아직 안 정해진 것

⬜ **결정하지 않은 것을 코드가 먼저 정하지 않게 한다.** 코드가 먼저 답을 정하면
그건 결정이 아니라 사고다.

- **에이전트의 직렬화 형식** — 손으로 짠 JSON 이냐 이진 프레임이냐.
  리뷰는 이진을 권했다(이진 본문·짝 없는 서로게이트에서 JSON 이 깨진다).
  v0 에는 손으로 짠 기록기가 없으므로 **v1 에서 정한다**
- **기록 보관 기간** — 지금은 총량 상한만 있다
- **기록 저장소** — 파일에서 DB 로 옮길 시점. v3 에서 화면이 필요해지면

## §7. 🔴 다음에 확인해야 계획이 유지되는 것

⚠️ **이 둘이 아니라고 나오면 계획을 다시 짠다.** 코드를 더 쌓기 전에 본다.

1. **`Filter` + `DataSource` 감싸기로 만든 기록으로 재생이 성립하는가**
   — 안 되면 순서를 뒤집은 근거(§2-1)가 사라진다
2. **SQL 실행 횟수로 N+1 을 잡는 오라클이 실제 JPA 앱에서 안정적인가**
   — 커넥션 풀 프록시와 드라이버가 둘 다 세이면 숫자가 두 배가 된다

둘 다 `demo-app` 이 있어야 확인된다. 그래서 다음에 만드는 것은 `demo-app` 이다.
