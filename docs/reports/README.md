# docs/reports — 그날그날 무슨 일이 있었나

**날짜 폴더 하나가 하루다.** 그 안에 주제별로 파일을 둔다.

```
docs/reports/
├── README.md                ← 이 파일. 색인
└── <YYYY-MM-DD>/
    ├── <주제>.md            그날 한 일 · 잰 것 · 나온 숫자
    └── troubleshooting/
        └── <증상>.md        막혔다가 푼 것
```

## 무엇을 어디에 적나

| 종류 | 자리 | 예 |
| --- | --- | --- |
| 「지금 무엇인가」 | `docs/PRD.md`·`DESIGN.md`·`DATA_CONTRACT.md`·`00_CODE_WALKTHROUGH.md` | 모듈이 몇 개인가 |
| 「왜 이렇게 정했나」 | `docs/rules/` — **주제별. 날짜 아님** | 왜 자바 21인가 |
| **「그날 무슨 일이 있었나」** | **여기 `<날짜>/`** | 실측 결과, 리뷰 |
| **「막혔다가 풀었다」** | **`<날짜>/troubleshooting/`** | 빌드가 왜 죽었나 |

🔴 **같은 내용을 두 곳에 적지 않는다.** 한쪽이 반드시 먼저 낡는다.
실측으로 설계가 바뀌면 **설계 문서를 고치고, 여기엔 「무엇을 재서 왜 바꿨나」만** 남긴다.

## 🔴 트러블슈팅 파일 이름은 «증상»으로 짓는다

다음에 찾을 때는 **증상으로 찾지 원인으로 찾지 않는다.** 원인을 알면 이미 해결한 것이다.

```
⭕ 테스트-출력의-한글이-깨진다.md
⭕ 빌드가-IBM_SEMERU-만-남기고-죽는다.md
❌ 인코딩-설정.md              ← 뭘 찾는지 모르면 못 찾는다
❌ foojay-플러그인-버전.md      ← 원인을 알아야 찾을 수 있다
```

**내용에 넣을 것**: 증상(실제 출력 그대로) → 원인 → 해결 → 🔴 **시도했다가 실패한 방법**.
마지막 것이 가장 값이 크다 — 다음 사람이 같은 막다른 길을 다시 걷지 않는다.

---

## 📅 날짜별

### [2026-09-16](2026-09-16/)
| 무엇 | 결과 |
| --- | --- |
| 🔴 [**남아 있던 구멍을 막고, 안 재던 둘을 쟀다**](2026-09-16/remaining-holes.md) | 검사 279→**307건**. 🔴 **가명화가 방아쇠와 요약층을 통째로 건너뛰고 있었다** — `dedupKey` 가 진입점을 그대로 이어 붙인 자리라 가장 놓치기 쉬웠다. 그리고 **클라이언트는 500 을 봤는데 기록에는 200** 이 적히고 있었다. 🎯 잰 둘: 동시 8스레드에서 건당 비용 **3.0배**(선형 이하) · 🔴 **IN 절 때문에 5번 반복된 것이 오라클에는 1번으로 보인다** |
| 🎯 [**v0 에서 코드로 남아 있던 것을 한 번에 끝냈다**](2026-09-16/v0-completion.md) | 검사 245→**279건**. 🔴 **채점 네 겹이 처음으로 «넷 다» 진짜 앱에서 돌았다** — ㉢ 이 못 돌던 이유는 이 앱의 시험 12개가 전부 Hindsight 실험이라 「앱이 원래 갖고 있던 테스트」가 0개였기 때문. `hs` 를 진짜 콘솔에서 처음 돌려 벽 셋을 넘었고 (한글·이모지는 **안 깨졌다**), PR 이 나가기 직전 유출 검사를 붙였다 — ⚠️ 시험이 내 가정 둘을 틀렸다고 말했다 |
| 🔧 [배치파일을 돌리면 주석 조각이 명령으로 실행된다](2026-09-16/troubleshooting/배치파일을-돌리면-주석-조각이-명령으로-실행된다.md) | LF 줄바꿈과 한글 주석, 둘 다 있어야 한다. `.gitattributes` 에 `eol=crlf` 가 «이미 있었는데도» 안 막혔다 — 그건 git 을 거칠 때 고쳐 주지, 방금 만든 파일은 안 지켜 준다 |
| 🔄 [**사람이 지키던 규율 둘을 빌드가 지키게 했다**](2026-09-16/rules-as-checks.md) | 검사 237→**245건**, 빌드가 지키는 검사 3→**4개**. 엿새 동안 「나중에 붙인다」로 남아 있던 패키지 의존 방향 검사를 붙였고, 넷 다 **일부러 깨뜨려서** 잡히는 걸 확인했다. 🔴 만들다가 하나 찾았다 — 본문에 UTF-8 로 못 적는 글자가 있으면 **기록이 통째로 날아가는데 오류는 「Input length = 1」 뿐**이라 디스크 문제처럼 읽혔다 |
| 🎯 [**생성된 테스트를 «진짜로» 돌렸다**](2026-09-16/baseline-fails.md) | **고리가 한 바퀴 돌았다.** 버그가 있는 코드에서 생성된 테스트가 실제로 **실패**하고, 고치면 **통과**한다 — 채점 네 겹의 첫 겹이 처음으로 «측정»됐다. ⚠️ 다만 「고친 코드」는 사람이 써 둔 것이라 **「LLM 이 고칠 수 있는가」의 증거는 아니다** |
| 🔄 [**`hs` 명령어**](2026-09-16/hs-cli.md) | 검사 208→**234건**. 🎯 **어제 만든 의존성 감시선이 오늘 울렸다** — picocli 를 넣으니 빌드가 막혔고, demo-app 이 실제로 끌고 가는 것도 확인했다. 그래서 의존성 없이 만들고 **설계 문서를 고쳤다** |
| 🔄 [**채점 결과를 PR 로**](2026-09-16/pull-request-draft.md) | 검사 192→**208건**. 🔴 조각 단언 15건이 전부 통과하는데도, **통째로 읽어 보니 기록이 버린 이벤트 17건이 PR 에 한 글자도 없었다** — 등급이 DIVERGED 라 PARTIAL 경로를 안 타서 빠졌다 |

### [2026-09-15](2026-09-15/)
| 무엇 | 결과 |
| --- | --- |
| 🔄 [**v0 기록기를 만들었다**](2026-09-15/v0-recorder.md) | 기록을 «만드는» 코드가 처음 생겼다. 검사 23→**67건**. 🎯 미뤄 뒀던 「모듈인가 패키지인가」를 **재서** 끝냈다(의존성 59→**59**, 늘어난 것 0). 🔴 **진짜 앱에 붙이자 따로 시험할 때는 안 보이던 결함 둘**이 나왔다 |
| 🔄 [**과적합을 막는 네 겹**](2026-09-15/four-layer-scoring.md) | 검사 177→**192건**. 🔴 **「안 돌렸다」를 「통과」로 접지 않는다.** 만들다가 둘을 잡았다 — 「왜 중간인지」가 통째로 사라지던 자리와, **내가 만든 flaky 검사**(큐가 차면 버리는 게 의도인데 「전부 담긴다」를 단언했다) |
| 🔄 [**채점표를 고치지 못하게 막았다**](2026-09-15/patch-guard.md) | 검사 150→**177건**. «가짜 LLM» 을 갈아 끼워 여섯 가지 패치로 시험했다 — 🔴 **진짜 고침에 테스트 삭제가 끼어 있으면 진짜 고침도 안 쓴다**. 금지 목록을 비우니 27건 중 9건이 실패해 검사가 진짜로 막는 것을 확인했다 |
| 🔄 [**기록을 「실패하는 JUnit 테스트」로**](2026-09-15/test-generation.md) | 검사 133→**150건**. 🔴 **기록에 «없는» 것은 단언하지 않는다** — 그러면 버그와 무관하게 실패해서 아무 잘못 없는 패치가 벌을 받는다. javac 로 실제 컴파일해 보니 **생성물이 혼자서는 안 돌았다**(기반 클래스가 추상) |
| 🔄 [**되돌리고 재생해서 채점까지**](2026-09-15/replay-restore.md) | 한 바퀴가 «돌았다». 검사 120→**133건**. 🔴 돌려 보니 **채점기가 잘못된 근거로 통과**하고 있었다 — 오라클이 데이터 계약을 어기고 요약층(남의 질의가 섞인 60초 창)을 문턱으로 썼고, 게다가 «구조적으로 실패할 수 없는» 기준이었다 |
| 🔄 [**재생 채점을 만들었다**](2026-09-15/replay-oracle.md) | 오라클 셋 + 등급 판정을 **순수 함수로**. 검사 80→**120건**. 🔴 만들기 직전에 **조용한 고장 하나를 피했다** — 질의 지문을 두 벌로 만들 뻔했고, 그랬으면 «모든» 재생이 채점 불가가 되면서도 오류가 안 났을 것이다 |
| 🔄 [**기록기 오버헤드를 처음 쟀다**](2026-09-15/recorder-overhead.md) | 목표 넷 중 **둘 지킴 · 하나는 못 지켰다가 고쳐서 지킴 · 하나는 못 쟀음.** 🎯 SQL 이벤트 **5.42µs → 0.46µs**(11.7배) — 비싼 자리를 «재서» 찾았다. 🔴 사고 때 파일 쓰기가 **12ms**, 요청 스레드가 이걸 기다린다 |
| 🔧 [기록의 방아쇠가 전부 `ServletException` 으로 찍힌다](2026-09-15/troubleshooting/기록의-방아쇠가-전부-ServletException-으로-찍힌다.md) | 서블릿이 예외를 감싸서 준다. 🔴 그 결과 **두 번째 사고부터 기록이 아예 안 생긴다** |
| 🔄 🔧 [PR 을 열려는데 `gh` 가 없다](2026-09-15/troubleshooting/PR을-열려는데-gh가-없다고-나온다.md) | 「Claude 가 PR 까지 연다」로 규율을 바꿔 봤다가 **같은 날 되돌렸다.** `gh` 로그인이 브라우저·코드 입력이라 비대화형으로 못 끝내서, 사람이 PR 을 여는 편이 단계가 적다. `gh` 2.100.0 은 깔아 뒀다 |

### [2026-09-11](2026-09-11/)
| 무엇 | 결과 |
| --- | --- |
| [SQL 세기 실측](2026-09-11/sql-count-experiment.md) | ✅ 21 = 1+20. **두 배로 안 세인다.** 🎯 「모양별 반복」이 더 나은 신호라는 걸 알아내 오라클을 다듬었다 |
| [Filter 포획 실측](2026-09-11/http-capture-experiment.md) | ✅ 재생 성립. 🔴 쓰기 요청을 채점할 방법이 없다고 봤다 — **실험 ⑦ 에서 뒤집힘** |
| 🔄 [**쓰기 재생 실측**](2026-09-11/write-replay-experiment.md) | 🔴 **위 결론을 뒤집었다.** 원인은 「쓰기라서」가 아니라 「안 되돌려서」. 되돌리니 **글자까지 같다** |
| 🔄 [**DB 복원 방법 실측**](2026-09-11/db-restore-experiment.md) | 후보 셋을 갈랐다 — 롤백은 카운터를 안 되돌려 재생이 4·5·6 으로 갈리고, 가짜 DB 는 정석 수정에 **0 / 1** 로 답을 못 준다. **스냅숏으로 결정.** 그리고 되돌릴 목록을 전수로 세었다(둘) |
| 🧭 [**인수인계**](2026-09-11/handoff.md) | **다음 세션이 이어받는 법.** 읽는 순서 · 환경 · 지금 손에 든 것 |

### [2026-09-10](2026-09-10/)
| 무엇 | 결과 |
| --- | --- |
| [설계 리뷰](2026-09-10/design-review.md) | 🔴 전제 넷이 무너졌다 — 오라클 없음 · 채점기가 옳은 패치를 벌줌 · 순서가 거꾸로 · 메모리 산수 10배 |

---

## 🔧 트러블슈팅 모아보기

날짜에 묻히면 못 찾으므로 여기 모은다.

| 증상 | 언제 |
| --- | --- |
| [`bin\hs.bat` 을 돌렸더니 주석 조각이 「명령」으로 실행된다](2026-09-16/troubleshooting/배치파일을-돌리면-주석-조각이-명령으로-실행된다.md) | 09-16 |
| [기록의 방아쇠가 전부 `ServletException` 으로 찍힌다 · 두 번째부터 기록이 안 생긴다](2026-09-15/troubleshooting/기록의-방아쇠가-전부-ServletException-으로-찍힌다.md) | 09-15 |
| [PR 을 열려는데 `gh: command not found` · 깔아도 `not logged into any GitHub hosts`](2026-09-15/troubleshooting/PR을-열려는데-gh가-없다고-나온다.md) | 09-15 |
| [테스트 출력의 한글이 `?? ?? ??` 로 깨진다](2026-09-11/troubleshooting/gradle-테스트-한글-깨짐.md) | 09-11 |
| [`JAVA_HOME is set to an invalid directory` — 경로는 맞는데 그렇게 나온다](2026-09-11/troubleshooting/JAVA_HOME이-올바른-디렉터리가-아니라고-나온다.md) | 09-11 |
| [`git push` 가 아무 말 없이 멈춘다 · WSL 에서는 `could not read Username`](2026-09-11/troubleshooting/git-push-가-아무-말-없이-멈춘다.md) | 09-11 |
| [빌드가 `IBM_SEMERU` 한 줄만 남기고 죽는다](2026-09-10/troubleshooting/gradle-빌드가-IBM_SEMERU-만-남기고-죽는다.md) | 09-10 |
| [`record` 에 없는 필드가 JSON 에 나타나서 왕복이 깨진다](2026-09-10/troubleshooting/record에-없는-필드가-JSON에-나타난다.md) | 09-10 |
